{
  "name": "steve-ui",
  "version": "1.0.0",
  "description": "1. Restore it (`dotnet restore`)\r 2. Build it (`dotnet build`)\r 3. Run it (`dotnet run`)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aspnet/signalr": "^1.0.0-preview2-30285"
  }
}
